{
  "name": "e2e",
  "version": "0.0.1",
  "private": true,
  "description": "Browser end-to-end tests for Vocab Bloom Hub: Playwright drives the admin UI against a real NestJS API and an isolated SQLite database.",
  "license": "MIT",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26"
  }
}
